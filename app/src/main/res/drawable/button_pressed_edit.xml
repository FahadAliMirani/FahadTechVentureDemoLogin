<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/button_pressed.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="100dp" />
            <solid android:color="#BDBDBD" /> <!-- Change the color to your desired pressed color -->
        </shape>
    </item>

    <!-- Focused state -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="100dp" />
            <solid android:color="#E0E0E0" /> <!-- Change the color to your desired focused color -->
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="100dp" />
            <solid android:color="#00C853" /> <!-- Change the color to your desired default color -->
        </shape>
    </item>
</selector>
